version: '3.8'

services:
  spleeter:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./audio:/audio
      - ./output:/output
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  audio_data:
  output_data: 